%If this configuration file is added to TeX's path, it can
%be used to set up the texosquery defaults for the installation.

% Default application:
\def\TeXOSInvokerName{texosquery-jre8}

% If texosquery-jre8 is added to the restricted list, we
% can switch off dry run mode if restricted mode detected using:
%\TeXOSQueryAllowRestricted
%(otherwise the dry run mode has to be switched off in the document)
