#!/bin/sh

# This bash script is just for testing the development
# texosquery-jre5.jar file. When installing the working application,
# please use the texosquery-jre5.sh script bundled in texosquery.dtx

bindir=`dirname $0`
java -jar "$bindir/texosquery-jre5.jar" "$@"

